 *to run karma*
 karma start karma.conf.js


 *If npm and bower not there*
 npm install
 
 -karma has separate cli like grunt that needed to be installed globally
 npm install -g karma-cli

 bower install then move bower_components out to root of project so tests and source code can find js files
